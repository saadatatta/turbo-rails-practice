
<%= turbo_stream.update nested_dom_id(@line_item_date, LineItem.new), "" %>

<%= turbo_stream.append nested_dom_id(@line_item_data, "line_items") do %>

<% end %>

<%= turbo_stream.update dom_id(@quote, :total) do %>
  <%= render "quotes/total", quote: @quote %>
<% end %>